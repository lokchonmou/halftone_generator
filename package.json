{
  "name": "halftone-generator-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && echo '🔧 Build finished. Push to trigger GitHub Pages workflow.'"
  },
  "dependencies": {
    "fflate": "^0.8.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.0.0"
  }
}
